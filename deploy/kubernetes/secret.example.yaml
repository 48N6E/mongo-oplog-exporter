# Example Secret — replace password before apply
# kubectl apply -f secret.example.yaml
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-exporter
type: Opaque
stringData:
  # Sidecar connects to localhost MongoDB in the same Pod
  mongo-url: "mongodb://exporter:change-me@127.0.0.1:27017/?authSource=admin"
